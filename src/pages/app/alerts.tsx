import MainLayout from "~/components/main-layout";

import { Alert_Operators_Enum } from "~/generated/graphql";

const AlertsPage = () => {
  return <MainLayout pageName="Alerts">Alerts</MainLayout>;
};

export default AlertsPage;
